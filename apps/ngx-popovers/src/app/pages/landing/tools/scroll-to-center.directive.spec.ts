import { ScrollToCenterDirective } from './scroll-to-center.directive';

describe('ScrollToCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToCenterDirective();
    expect(directive).toBeTruthy();
  });
});
