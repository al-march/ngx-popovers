import { DialogTriggerDirective } from './dialog-trigger.directive';

describe('DialogTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
