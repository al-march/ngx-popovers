import { PopoverTemplate } from './popover-template.directive';

describe('PopoverContentDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverTemplate();
    expect(directive).toBeTruthy();
  });
});
