import { PopoverAnchorDirective } from './popover-anchor.directive';

describe('PopoverAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
