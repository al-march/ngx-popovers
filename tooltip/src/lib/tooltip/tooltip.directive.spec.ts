import { NgxTooltip } from './tooltip.directive';

describe('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTooltip();
    expect(directive).toBeTruthy();
  });
});
