import { DialogTemplate } from './dialog-template.directive';

describe('DialogTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogTemplate();
    expect(directive).toBeTruthy();
  });
});
