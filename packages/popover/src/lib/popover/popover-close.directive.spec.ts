import { PopoverCloseDirective } from './popover-close.directive';

describe('PopoverCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverCloseDirective();
    expect(directive).toBeTruthy();
  });
});
