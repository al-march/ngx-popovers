import { PopoverClose } from './popover-close.directive';

describe('PopoverCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverClose();
    expect(directive).toBeTruthy();
  });
});
